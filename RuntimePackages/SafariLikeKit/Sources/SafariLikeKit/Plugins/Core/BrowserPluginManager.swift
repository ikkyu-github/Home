import SafariLikeCoreKit
// Intentionally empty.
//
// `NextGenPluginManager` (and the rest of the plugin runtime) is intentionally
// centralized in `Plugins/Runtime/PluginHost.swift` to enforce the
// “ONE PluginHost, MANY policies” architecture and prevent horizontal file
// growth.
