import SafariLikeContracts

/// SafariLikeUIKit does not provide a concrete thumbnail store.
///
/// Inject a runtime implementation (e.g. `SafariLikeKit.TabThumbnailStore`) via
/// `SafariLikeUIKitFactory(thumbnailStoreProvider:)`.
