import Foundation

// Intentionally minimal for now.
// This module is the neutral home for shared protocols/interfaces to avoid
// circular dependencies between UI (SwiftUI/UIKit) and core/runtime layers.
